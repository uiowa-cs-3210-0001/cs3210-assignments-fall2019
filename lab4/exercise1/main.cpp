#include <iostream>

// Write two versions of a function `square`, 
// one that operates on integers and one 
// that operates on floating-point numbers

int main() {
    std::cout << square( 2 ) << std::endl;
    std::cout << square( 2.2 ) << std::endl;
}
